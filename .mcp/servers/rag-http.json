{
  "name": "rag-http",
  "description": "Pipeline RAG multimodelo via HTTP",
  "entry": "http://127.0.0.1:8000",
  "tools": {
    "query": {
      "method": "POST",
      "path": "/query",
      "params": ["question", "k"]
    },
    "add_docs": {
      "method": "POST",
      "path": "/add_documents",
      "params": ["documents"]
    }
  }
} 